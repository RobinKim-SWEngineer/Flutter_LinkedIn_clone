import 'package:flutter/material.dart';
import 'package:linkedin_clone/common/preparation.dart';

class Body extends StatelessWidget {
  const Body({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Preparation();
  }
}
